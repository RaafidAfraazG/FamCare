package com.famcare.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class EmailConfig {
    // Email configuration already in application.properties
    // This class is just for organization
}